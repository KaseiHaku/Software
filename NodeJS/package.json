{
  "name": "packageName", // package的名字（由于他会成为url的一部分，所以 non-url-safe 的字母不会通过，也不允许出现"."、"_"），最好先在 http://registry.npmjs.org/ 上搜下你取的名字是否已经存在
  "version": "version",
  "dependencies": {  // package 的应用依赖模块，即别人要使用这个package，至少需要安装哪些东东。应用依赖模块会安装到当前模块的 node_modules 目录下。
    
  },
  "devDependencies": {    // package 的开发依赖模块，即别人要在这个 package 上进行开发所需要的依赖
    
  }

}
