shell> cd $KAFKA_HOME
shell> bin/kafka-topics.sh --list --zookeeper localhost:2181    # 查询当前所有 topic
shell> bin/kafka-console-producer.sh --broker-list localhost:9092 --topic test   # 发送一条消息到指定 topic
shell> bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic test --from-beginning # 查看一个 topic 所有消息

